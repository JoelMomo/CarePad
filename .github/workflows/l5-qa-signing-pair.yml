name: L5 QA signed pair

on:
  pull_request:
    branches:
      - main
    paths:
      - .github/workflows/l5-qa-signing-pair.yml
      - .github/qa/l5-qa-debug.keystore.b64
      - .github/qa/L5_QA_SIGNING.md

permissions:
  contents: read

env:
  BASELINE_SHA: fea6a1cf2274ed5d2b92d829aa5fbed7b39aa0b4
  CANDIDATE_SHA: a10a062b660f90ba6ec0d014d118081c9fa97f32
  EXPECTED_CERT_SHA256: 8513b30c18d3bb7574830a830461c862fe26c124dd1b1c694f7f58af271662e6

jobs:
  build-same-signer-pair:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout QA infrastructure
        uses: actions/checkout@v4

      - name: Checkout exact baseline
        uses: actions/checkout@v4
        with:
          ref: ${{ env.BASELINE_SHA }}
          path: baseline
          persist-credentials: false

      - name: Checkout exact candidate
        uses: actions/checkout@v4
        with:
          ref: ${{ env.CANDIDATE_SHA }}
          path: candidate
          persist-credentials: false

      - name: Verify exact source SHAs
        run: |
          set -euo pipefail
          test "$(git -C baseline rev-parse HEAD)" = "$BASELINE_SHA"
          test "$(git -C candidate rev-parse HEAD)" = "$CANDIDATE_SHA"

      - name: Set up Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android build SDK
        run: sdkmanager "platforms;android-36" "build-tools;36.0.0"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Install reproducible QA signer
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/l5-qa-pair"
          base64 --decode .github/qa/l5-qa-debug.keystore.b64 > "$RUNNER_TEMP/l5-qa-pair/qa-debug.keystore"
          chmod 600 "$RUNNER_TEMP/l5-qa-pair/qa-debug.keystore"

          CERT_SHA256="$(keytool -list -v \
            -keystore "$RUNNER_TEMP/l5-qa-pair/qa-debug.keystore" \
            -storepass android \
            -alias androiddebugkey \
            | awk '/SHA256:/{gsub(":", "", $2); print tolower($2); exit}')"

          echo "qa_signer_sha256=$CERT_SHA256"
          test "$CERT_SHA256" = "$EXPECTED_CERT_SHA256"

      - name: Build exact baseline and candidate debug APKs
        run: |
          set -euo pipefail
          chmod +x baseline/gradlew candidate/gradlew
          ./baseline/gradlew -p baseline :app:assembleDebug --stacktrace
          ./candidate/gradlew -p candidate :app:assembleDebug --stacktrace

      - name: Re-sign both APKs explicitly with QA signer
        run: |
          set -euo pipefail

          APKSIGNER="$ANDROID_HOME/build-tools/36.0.0/apksigner"
          QA_KEYSTORE="$RUNNER_TEMP/l5-qa-pair/qa-debug.keystore"
          BASE_RAW="$GITHUB_WORKSPACE/baseline/app/build/outputs/apk/debug/app-debug.apk"
          CANDIDATE_RAW="$GITHUB_WORKSPACE/candidate/app/build/outputs/apk/debug/app-debug.apk"
          BASE_APK="$RUNNER_TEMP/l5-qa-pair/CarePad-L5-QA-baseline-${BASELINE_SHA}.apk"
          CANDIDATE_APK="$RUNNER_TEMP/l5-qa-pair/CarePad-L5-QA-candidate-${CANDIDATE_SHA}.apk"

          "$APKSIGNER" sign \
            --ks "$QA_KEYSTORE" \
            --ks-key-alias androiddebugkey \
            --ks-pass pass:android \
            --key-pass pass:android \
            --out "$BASE_APK" \
            "$BASE_RAW"

          "$APKSIGNER" sign \
            --ks "$QA_KEYSTORE" \
            --ks-key-alias androiddebugkey \
            --ks-pass pass:android \
            --key-pass pass:android \
            --out "$CANDIDATE_APK" \
            "$CANDIDATE_RAW"

      - name: Verify package, signer and APK digests
        run: |
          set -euo pipefail

          BASE_APK="$RUNNER_TEMP/l5-qa-pair/CarePad-L5-QA-baseline-${BASELINE_SHA}.apk"
          CANDIDATE_APK="$RUNNER_TEMP/l5-qa-pair/CarePad-L5-QA-candidate-${CANDIDATE_SHA}.apk"
          APKSIGNER="$ANDROID_HOME/build-tools/36.0.0/apksigner"
          APKANALYZER="$(find "$ANDROID_HOME/cmdline-tools" -type f -name apkanalyzer -print -quit)"

          "$APKSIGNER" verify --print-certs "$BASE_APK" | tee "$RUNNER_TEMP/l5-qa-pair/baseline-certs.txt"
          "$APKSIGNER" verify --print-certs "$CANDIDATE_APK" | tee "$RUNNER_TEMP/l5-qa-pair/candidate-certs.txt"

          BASE_CERT="$(awk -F': ' 'tolower($0) ~ /certificate sha-256 digest/ {print tolower($NF); exit}' "$RUNNER_TEMP/l5-qa-pair/baseline-certs.txt")"
          CANDIDATE_CERT="$(awk -F': ' 'tolower($0) ~ /certificate sha-256 digest/ {print tolower($NF); exit}' "$RUNNER_TEMP/l5-qa-pair/candidate-certs.txt")"
          test "$BASE_CERT" = "$EXPECTED_CERT_SHA256"
          test "$CANDIDATE_CERT" = "$EXPECTED_CERT_SHA256"
          test "$BASE_CERT" = "$CANDIDATE_CERT"

          BASE_PACKAGE="$("$APKANALYZER" manifest application-id "$BASE_APK")"
          CANDIDATE_PACKAGE="$("$APKANALYZER" manifest application-id "$CANDIDATE_APK")"
          test -n "$BASE_PACKAGE"
          test "$BASE_PACKAGE" = "$CANDIDATE_PACKAGE"

          BASE_VERSION="$("$APKANALYZER" manifest version-code "$BASE_APK")"
          CANDIDATE_VERSION="$("$APKANALYZER" manifest version-code "$CANDIDATE_APK")"
          BASE_APK_SHA256="$(sha256sum "$BASE_APK" | awk '{print $1}')"
          CANDIDATE_APK_SHA256="$(sha256sum "$CANDIDATE_APK" | awk '{print $1}')"

          cat > "$RUNNER_TEMP/l5-qa-pair/signing-report.txt" <<EOF
          baseline_source_sha=$BASELINE_SHA
          candidate_source_sha=$CANDIDATE_SHA
          baseline_apk_sha256=$BASE_APK_SHA256
          candidate_apk_sha256=$CANDIDATE_APK_SHA256
          baseline_certificate_sha256=$BASE_CERT
          candidate_certificate_sha256=$CANDIDATE_CERT
          package_name=$BASE_PACKAGE
          baseline_version_code=$BASE_VERSION
          candidate_version_code=$CANDIDATE_VERSION
          adb_install_r_cryptographically_possible=true
          reason=same_package_and_same_signing_certificate
          signer_scope=QA_LAB_ONLY_NOT_FOR_RELEASE
          signing_method=explicit_apksigner_resign_after_exact_source_build
          EOF

          cat "$RUNNER_TEMP/l5-qa-pair/signing-report.txt"

      - name: Upload exact baseline QA APK
        uses: actions/upload-artifact@v4
        with:
          name: CarePad-L5-QA-baseline-same-signer
          path: ${{ runner.temp }}/l5-qa-pair/CarePad-L5-QA-baseline-*.apk
          if-no-files-found: error
          retention-days: 7

      - name: Upload exact candidate QA APK
        uses: actions/upload-artifact@v4
        with:
          name: CarePad-L5-QA-candidate-same-signer
          path: ${{ runner.temp }}/l5-qa-pair/CarePad-L5-QA-candidate-*.apk
          if-no-files-found: error
          retention-days: 7

      - name: Upload signing verification report
        uses: actions/upload-artifact@v4
        with:
          name: CarePad-L5-QA-signing-report
          path: |
            ${{ runner.temp }}/l5-qa-pair/signing-report.txt
            ${{ runner.temp }}/l5-qa-pair/baseline-certs.txt
            ${{ runner.temp }}/l5-qa-pair/candidate-certs.txt
          if-no-files-found: error
          retention-days: 7
